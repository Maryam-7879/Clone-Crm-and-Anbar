# سیستم مدیریت ارتباط با مشتری (CRM) + مدیریت انبار

سیستم جامع مدیریت CRM با قابلیت‌های کامل مدیریت موجودی و انبار

## 📋 فهرست مطالب

- [نصب و راه‌اندازی](#نصب-و-راه‌اندازی)
- [اطلاعات ورود پیش‌فرض](#اطلاعات-ورود-پیش‌فرض)
- [راه‌اندازی دیتابیس](#راه‌اندازی-دیتابیس)
- [تنظیمات سیستم](#تنظیمات-سیستم)
- [امکانات سیستم](#امکانات-سیستم)
- [راهنمای استفاده](#راهنمای-استفاده)

---

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها

- PHP 7.4 یا بالاتر
- MySQL 5.7 یا بالاتر (یا MariaDB 10.2+)
- Apache/Nginx با mod_rewrite
- XAMPP / WAMP / LAMP (برای محیط توسعه)

### مراحل نصب

1. **کپی کردن فایل‌ها**
   ```
   پروژه را در پوشه htdocs کپی کنید:
   /Applications/XAMPP/xamppfiles/htdocs/project-comp/new-crm/
   ```

2. **تنظیمات Apache**
   - اطمینان حاصل کنید که mod_rewrite فعال است
   - DocumentRoot را به پوشه public تنظیم کنید

3. **راه‌اندازی دیتابیس** (مطابق بخش زیر)

---

## 🔑 اطلاعات ورود پیش‌فرض

بعد از نصب و راه‌اندازی دیتابیس، می‌توانید با اطلاعات زیر وارد سیستم شوید:

### مدیر سیستم (Admin)
- **نام کاربری:** `admin`
- **رمز عبور:** `password`
- **ایمیل:** admin@crm.com
- **نقش:** مدیر کل سیستم

### مدیر فروش (Manager)
- **نام کاربری:** `manager`
- **رمز عبور:** `password`
- **ایمیل:** manager@crm.com
- **نقش:** مدیر فروش

### کارشناس فروش (Sales)
- **نام کاربری:** `sales1`
- **رمز عبور:** `password`
- **ایمیل:** sales@crm.com
- **نقش:** کارشناس فروش

### کاربر عادی (User)
- **نام کاربری:** `user1`
- **رمز عبور:** `password`
- **ایمیل:** user@crm.com
- **نقش:** کاربر عادی

> ⚠️ **هشدار امنیتی:** بعد از اولین ورود، حتماً رمز عبور همه کاربران را تغییر دهید!

---

## 💾 راه‌اندازی دیتابیس

### روش 1: استفاده از phpMyAdmin (توصیه شده)

1. **باز کردن phpMyAdmin**
   ```
   آدرس: http://localhost/phpmyadmin
   ```

2. **ایجاد دیتابیس جدید**
   - کلیک روی "New" یا "ایجاد دیتابیس"
   - نام دیتابیس: `crm_system_new`
   - Collation: `utf8mb4_unicode_ci`
   - کلیک روی "Create"

3. **Import کردن فایل schema.sql**
   - انتخاب دیتابیس `crm_system_new`
   - رفتن به تب "Import"
   - کلیک روی "Choose File" و انتخاب فایل:
     ```
     database/schema.sql
     ```
   - کلیک روی "Go" برای شروع import
   - منتظر بمانید تا پیام موفقیت نمایش داده شود

### روش 2: استفاده از Command Line

**در Windows:**
```bash
# ورود به پوشه پروژه
cd C:\xampp\htdocs\project-comp\new-crm

# اجرای دستورات MySQL
C:\xampp\mysql\bin\mysql.exe -u root -p -e "CREATE DATABASE IF NOT EXISTS crm_system_new CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# Import کردن schema
C:\xampp\mysql\bin\mysql.exe -u root -p crm_system_new < database\schema.sql
```

**در Linux/Mac:**
```bash
# ورود به پوشه پروژه
cd /Applications/XAMPP/xamppfiles/htdocs/project-comp/new-crm

# اجرای دستورات MySQL
mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS crm_system_new CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# Import کردن schema
mysql -u root -p crm_system_new < database/schema.sql
```

### روش 3: استفاده از فایل reset_database.sql

اگر می‌خواهید دیتابیس را از ابتدا بسازید:

1. باز کردن فایل `database/reset_database.sql`
2. کپی کردن محتوا
3. اجرا در phpMyAdmin یا MySQL Command Line

### بررسی صحت نصب

بعد از import، باید جداول زیر در دیتابیس ایجاد شده باشند:

**جداول CRM:**
- `users` - کاربران سیستم
- `customers` - مشتریان
- `leads` - لیدها
- `tasks` - وظایف
- `sales` - فروش‌ها
- `sale_items` - آیتم‌های فروش
- `payments` - پرداخت‌ها
- `customer_activities` - فعالیت‌های مشتریان

**جداول موجودی:**
- `categories` - دسته‌بندی‌ها
- `suppliers` - تامین‌کنندگان
- `products` - محصولات
- `inventories` - موجودی
- `transactions` - تراکنش‌های موجودی
- `alerts` - هشدارها

---

## ⚙️ تنظیمات سیستم

### تنظیمات دیتابیس

فایل `private/config.php` را ویرایش کنید:

```php
// تنظیمات دیتابیس
define('DB_HOST', 'localhost');        // آدرس سرور دیتابیس
define('DB_NAME', 'crm_system_new');   // نام دیتابیس
define('DB_USER', 'root');             // نام کاربری MySQL
define('DB_PASS', '');                 // رمز عبور MySQL
```

### تنظیمات URL پایه

```php
define('BASE_URL', 'http://localhost/project-comp/new-crm/public/');
```

> توجه: در محیط production، آدرس را به دامنه اصلی تغییر دهید.

### تنظیمات امنیتی

```php
// کلید رمزنگاری (حتماً در production تغییر دهید!)
define('ENCRYPTION_KEY', 'CRM_SECRET_KEY_2024_SECURE_RANDOM_STRING_HERE');

// زمان انقضای session (ثانیه)
define('SESSION_TIMEOUT', 3600); // 1 ساعت

// حداکثر تلاش برای ورود
define('MAX_LOGIN_ATTEMPTS', 5);
```

### تنظیمات فایل آپلود

```php
define('UPLOAD_MAX_SIZE', 5242880); // 5MB
define('UPLOAD_ALLOWED_TYPES', ['jpg', 'jpeg', 'png', 'pdf', 'doc', 'docx']);
define('UPLOAD_PATH', 'uploads/');
```

> اطمینان حاصل کنید که پوشه `uploads/` قابل نوشتن است (chmod 755)

---

## 📦 امکانات سیستم

### مدیریت CRM

#### 1. مدیریت مشتریان
- افزودن، ویرایش و حذف مشتریان
- ثبت اطلاعات تماس و آدرس
- ردیابی فعالیت‌های مشتریان
- دسته‌بندی بر اساس نوع (فردی/شرکتی)

#### 2. مدیریت لیدها
- ثبت و پیگیری لیدها
- مدیریت وضعیت لیدها (جدید، تماس گرفته شده، واجد شرایط، پیشنهاد، مذاکره، موفق، از دست رفته)
- تعیین اولویت (کم، متوسط، بالا، فوری)
- قیف تبدیل لید به مشتری

#### 3. مدیریت فروش
- ثبت فروش‌ها و سفارشات
- مدیریت آیتم‌های فروش
- ثبت پرداخت‌ها
- چاپ فاکتور

#### 4. مدیریت وظایف
- ایجاد و تخصیص وظایف
- تنظیم یادآوری
- ردیابی وضعیت وظایف
- ارتباط با مشتریان و لیدها

### مدیریت موجودی و انبار

#### 1. مدیریت دسته‌بندی‌ها
- ایجاد دسته‌بندی با رنگ و آیکون
- مدیریت سلسله مراتبی دسته‌بندی‌ها

#### 2. مدیریت تامین‌کنندگان
- ثبت اطلاعات تامین‌کنندگان
- مدیریت تماس‌ها و آدرس‌ها

#### 3. مدیریت محصولات
- ثبت محصولات با تمام جزئیات (SKU, بارکد، تصویر)
- تعیین قیمت خرید و فروش
- تنظیم حداقل و حداکثر موجودی
- مدیریت وضعیت فعال/غیرفعال

#### 4. مدیریت موجودی
- ردیابی موجودی فعلی، رزرو و موجود
- تعدیل موجودی
- محاسبه میانگین هزینه

#### 5. تراکنش‌های موجودی
- ثبت ورود کالا
- ثبت خروج کالا
- تعدیل موجودی
- انتقال بین انبارها
- گزارش‌گیری از تراکنش‌ها

#### 6. سیستم هشدار
- هشدار موجودی کم
- هشدار تمام شدن موجودی
- هشدارهای سفارشی
- مدیریت وضعیت خوانده شدن

### گزارش‌گیری و آنالیتیکس

- گزارش‌های فروش ماهانه
- گزارش عملکرد کاربران
- گزارش موجودی و تراکنش‌ها
- گزارش محصولات پرفروش
- گزارش موجودی بر اساس دسته‌بندی
- نمودارها و چارت‌های تعاملی

---

## 📖 راهنمای استفاده

### ورود به سیستم

1. باز کردن مرورگر و رفتن به آدرس:
   ```
   http://localhost/project-comp/new-crm/public/
   ```
   یا
   ```
   http://localhost/project-comp/new-crm/public/index.php
   ```

2. وارد کردن نام کاربری و رمز عبور پیش‌فرض

3. کلیک روی "ورود"

### داشبورد اصلی

بعد از ورود، داشبورد شامل موارد زیر است:

- **آمار کلی CRM:** مشتریان، لیدها، فروش، وظایف
- **آمار موجودی:** محصولات، موجودی کم، ارزش کل موجودی
- **نمودار فروش ماهانه**
- **لیدهای فوری**
- **وظایف امروز**
- **آخرین تراکنش‌های موجودی**
- **هشدارهای جدید**
- **محصولات با موجودی کم**
- **آخرین فعالیت‌ها**

### افزودن مشتری جدید

1. رفتن به منوی "مشتریان"
2. کلیک روی "افزودن مشتری جدید"
3. پر کردن فرم (فیلدهای الزامی: نام، نام خانوادگی)
4. کلیک روی "ذخیره"

### ثبت لید جدید

1. رفتن به منوی "لیدها"
2. کلیک روی "افزودن لید جدید"
3. پر کردن اطلاعات لید
4. تعیین وضعیت و اولویت
5. ذخیره

### ثبت محصول جدید

1. رفتن به منوی "محصولات"
2. کلیک روی "افزودن محصول جدید"
3. پر کردن اطلاعات محصول (نام، SKU، قیمت)
4. انتخاب دسته‌بندی و تامین‌کننده
5. تنظیم حداقل موجودی
6. آپلود تصویر (اختیاری)
7. ذخیره

### ثبت تراکنش موجودی

1. رفتن به منوی "موجودی"
2. انتخاب محصول
3. کلیک روی "تعدیل موجودی"
4. انتخاب نوع تراکنش (ورود، خروج، تعدیل)
5. وارد کردن تعداد و قیمت
6. اضافه کردن توضیحات
7. ثبت

### مشاهده گزارش‌ها

1. رفتن به منوی "گزارش‌ها"
2. انتخاب بازه زمانی (7 روز، 30 روز، 90 روز، 1 سال)
3. مشاهده گزارش‌های مختلف:
   - گزارش‌های CRM (مشتریان، لیدها، فروش)
   - گزارش‌های موجودی (محصولات، تراکنش‌ها)
   - نمودارها و جداول تحلیلی

### مدیریت کاربران

> فقط مدیر سیستم (admin) می‌تواند کاربران جدید اضافه کند.

1. رفتن به منوی "کاربران"
2. کلیک روی "افزودن کاربر جدید"
3. پر کردن اطلاعات کاربر
4. تعیین نقش (admin, manager, sales, user)
5. ذخیره

---

## 🔒 امنیت

### توصیه‌های امنیتی

1. **تغییر رمز عبور پیش‌فرض**
   - بلافاصله بعد از نصب، رمز عبور تمام کاربران را تغییر دهید

2. **تغییر کلید رمزنگاری**
   - در فایل `config.php`، `ENCRYPTION_KEY` را به یک رشته تصادفی تغییر دهید

3. **تنظیمات دیتابیس**
   - از کاربر root در production استفاده نکنید
   - کاربر دیتابیس را با دسترسی محدود ایجاد کنید

4. **تنظیمات سرور**
   - در production، `display_errors` را روی `0` قرار دهید
   - پوشه `private/` را خارج از DocumentRoot قرار دهید (در صورت امکان)

5. **پشتیبان‌گیری**
   - به صورت منظم از دیتابیس backup بگیرید

---

## 🐛 عیب‌یابی

### خطای اتصال به دیتابیس

**مشکل:** "خطا در اتصال به دیتابیس"

**راه‌حل:**
1. بررسی کنید MySQL در حال اجرا است
2. تنظیمات دیتابیس در `config.php` را بررسی کنید
3. اطمینان حاصل کنید که دیتابیس `crm_system_new` ایجاد شده است

### خطای "Table doesn't exist"

**مشکل:** "Table 'crm_system_new.users' doesn't exist"

**راه‌حل:**
1. فایل `database/schema.sql` را مجدداً import کنید
2. بررسی کنید که تمام جداول ایجاد شده‌اند

### خطای "Permission denied"

**مشکل:** خطا در آپلود فایل یا نوشتن در پوشه

**راه‌حل:**
```bash
# تنظیم دسترسی پوشه uploads
chmod -R 755 uploads/
chown -R www-data:www-data uploads/
```

### صفحه سفید (White Screen)

**مشکل:** صفحه نمایش داده نمی‌شود

**راه‌حل:**
1. بررسی فایل `logs/error.log`
2. در `config.php`، `display_errors` را روی `1` قرار دهید (فقط برای debug)
3. بررسی کنید که PHP Extension‌های لازم فعال هستند (PDO, pdo_mysql)

---

## 📝 داده‌های تستی

بعد از import کردن `schema.sql`، داده‌های تستی زیر اضافه می‌شوند:

### کاربران
- 4 کاربر با نقش‌های مختلف

### مشتریان
- 5 مشتری (فردی و شرکتی)

### لیدها
- 5 لید با وضعیت‌های مختلف

### وظایف
- 5 وظیفه

### موجودی
- 5 دسته‌بندی
- 5 تامین‌کننده
- 10 محصول
- 10 رکورد موجودی
- 13 تراکنش
- 5 هشدار

### فروش
- 3 فروش
- آیتم‌های فروش و پرداخت‌های مرتبط

---

## 📞 پشتیبانی

برای دریافت کمک:
1. بررسی فایل `logs/error.log` برای خطاها
2. بررسی مستندات بالا
3. بررسی کد و کامنت‌ها

---

## 📄 لایسنس

این پروژه برای استفاده داخلی طراحی شده است.

---

## 🔄 نسخه‌ها

- **نسخه فعلی:** 1.0.0
- **تاریخ به‌روزرسانی:** 2024

---

## ✅ چک‌لیست نصب

- [ ] PHP 7.4+ نصب شده
- [ ] MySQL 5.7+ نصب و در حال اجرا
- [ ] فایل‌های پروژه در پوشه مناسب قرار دارند
- [ ] دیتابیس `crm_system_new` ایجاد شده
- [ ] فایل `schema.sql` import شده
- [ ] تنظیمات `config.php` انجام شده
- [ ] پوشه `uploads/` قابل نوشتن است
- [ ] ورود به سیستم با اکانت admin موفق بوده
- [ ] رمز عبور پیش‌فرض تغییر کرده
- [ ] کلید رمزنگاری تغییر کرده

---

**موفق باشید! 🎉**
